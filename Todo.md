# Apple-like UI 改进清单

## 目标

将当前界面从“使用 Apple 配色与圆角的通用后台页面”提升为更接近 Apple 产品的体验：内容优先、层级清晰、留白有节奏、材质克制、交互自然，并同时保证中文阅读、深色模式、移动端和可访问性。

建议的视觉方向是 **Apple.com 的排版与留白 + macOS 工具类应用的高效和克制**，不照搬拟物窗口、交通灯等装饰。

## 当前界面诊断

- [ ] 页面整体虽然干净，但首页有效内容只占首屏很小区域，下方出现大面积无意义空白，视觉重心偏上且偏弱。
- [ ] 目前的 Apple 感主要来自 `#f5f5f7`、系统字体、蓝色、圆角和阴影，缺少更关键的排版比例、空间节奏和内容层级。
- [ ] `rounded-full`、`rounded-3xl`、彩色浅底和阴影使用较多，所有元素都很“软”，缺少主次对比；Apple 风格应克制使用胶囊和悬浮感。
- [ ] 全局 `Card` 对非交互内容也提供 hover 阴影，容易产生“可以点击”的错误暗示。
- [ ] 首页标题、顶栏品牌和项目卡片的视觉权重接近，没有形成明确的阅读路径。
- [ ] 卡片中的文件名、日期、状态、操作、统计和进度都挤在同一层级，信息可扫读性一般。
- [ ] 审核页将导航、审核结论、备注和反馈集中在一张大卡内，核心决策不够突出，长任务文本的阅读体验也不稳定。
- [ ] 结果页同时使用四种彩色统计块、圆环进度和线性进度，信息重复且颜色偏多，不符合 Apple 界面的克制感。
- [ ] 上传与保存反馈会占据文档流并造成布局跳动，缺少精致、稳定的过程反馈。
- [ ] 现有响应式主要依赖简单折行，尚未为窄屏重新安排操作优先级、按钮宽度和信息密度。

## P0：先统一视觉系统

### 设计 Token

- [ ] 将颜色变量扩展为语义化层级，而不是在页面中反复拼透明度：
  - `--background`
  - `--surface-primary`
  - `--surface-secondary`
  - `--surface-elevated`
  - `--fill`
  - `--separator`
  - `--label-primary`
  - `--label-secondary`
  - `--label-tertiary`
  - `--tint`
- [ ] 为亮色与深色主题分别校准分隔线、悬浮面、输入框和禁用态，避免仅用同一颜色叠加透明度。
- [ ] 统一圆角层级，减少随意使用：
  - 页面主卡片：18–20px
  - 普通容器/输入框：12–14px
  - 小按钮：10–12px
  - 仅状态标签、分段控制器和明确的胶囊按钮使用全圆角
- [ ] 统一阴影层级：
  - 默认卡片以细边框和色阶分层为主
  - 只有浮层、上传弹窗或真正可点击的悬浮对象使用明显阴影
  - 深色模式避免大面积黑色外阴影，改用高光边框和表面色阶
- [ ] 建立 4/8px 间距体系，并为页面、区块、组件分别定义稳定的间距档位。
- [ ] 增加动效 Token：快速反馈约 120–160ms，页面/浮层约 220–300ms，统一使用自然的缓动曲线。

### 字体与排版

- [ ] 英文继续使用 SF Pro 系统栈；中文明确加入 `"PingFang SC"`、`"Hiragino Sans GB"`、`"Microsoft YaHei"` 回退。
- [ ] 建立清晰的字体层级，而不是各页面独立选择字号：
  - 页面标题：40–48px / 紧凑行高
  - 区块标题：24–28px
  - 卡片标题：17–19px
  - 正文：15–17px
  - 辅助信息：12–14px
- [ ] 中文标题避免过度负字距；数字进度使用 `tabular-nums`，让变化时更稳定。
- [ ] 长任务正文限制理想行长约 32–42 个中文字符，并根据内容长度在 18–24px 范围自适应，避免当前固定大字号造成短句空、长句累。

### 基础组件

- [ ] 重构 `Card`：增加 `interactive` 属性，仅交互卡片提供 hover/press/focus 反馈；静态卡片不悬浮。
- [ ] 重构 `Button`：
  - 主按钮使用 Apple 蓝，仅用于每个视图的一个主操作
  - 次级按钮改为浅灰填充或纯文本
  - 危险/警告色只表达真实风险，不将“暂时遗留”做成过强的黄色按钮
  - 所有触控目标至少 44×44px
- [ ] 将主题选择器做成真正的 macOS 分段控制器：等宽选项、清晰的选中滑块、统一 44px 触控区域。
- [ ] 为输入框补充 hover、focus、error、disabled 状态，聚焦时采用细边框 + 柔和外环，避免粗重蓝圈。
- [ ] 状态 Badge 统一高度、内边距、圆点与文字基线；降低饱和度，保证状态文字而非颜色承担主要语义。
- [ ] 增加统一的 Toast/Inline Notice、Skeleton、Empty State 和 Modal/Sheet 组件。
- [ ] 所有动效支持 `prefers-reduced-motion`。

## P0：首页 `/`

- [ ] 将页面容器由当前偏窄的 `max-w-4xl` 调整为约 1040–1120px，使桌面端内容与顶栏形成统一网格。
- [ ] 重做首屏标题区：
  - 增加小型 eyebrow 或简短产品说明
  - 标题和说明形成明显字号差
  - 上传入口与标题基线对齐
  - 控制首屏高度，不让项目列表显得孤立
- [ ] 顶栏品牌改为更精致的图标 + 产品名组合，弱化与页面标题的竞争；滚动时再显示半透明材质和分隔线，页面顶部可更轻。
- [ ] 将项目列表由“漂浮大卡片”改为更接近 macOS 列表/设置项的组合：
  - 外层可以是一张统一面板
  - 项目之间使用轻分隔线
  - 每行保持 72–88px 高度
  - hover 仅改变浅背景，减少每张卡独立大阴影
- [ ] 优化项目信息层级：
  - 第一行：项目名 + 状态
  - 第二行：文件名 + 创建日期
  - 右侧：进度百分比 + 主操作
  - 底部或次级行：通过/暂留/待处理统计
- [ ] 让可进入的项目行整体可点击，并保留明确操作按钮；当前卡片 hover 会暗示整卡可点，但实际上只有按钮可点。
- [ ] 进度条提升到 4–6px，并让进度数字与进度条形成一组，避免数字悬在远端。
- [ ] 空状态改为更有品牌感的轻量插画/文档堆叠图形，附带唯一主按钮；避免大面积白卡片再套圆形图标的模板感。
- [ ] 上传改为 Sheet/Modal 或拖放区，展示所选文件名、大小、校验、上传/解析阶段和错误恢复入口。
- [ ] 上传成功与失败通过 Toast 或固定高度状态区域反馈，避免按钮下方出现文字导致标题区跳动。
- [ ] 对解析中项目使用低干扰 Skeleton/旋转状态，并自动刷新或提供刷新动作。

## P0：审核页 `/projects/[projectId]/review/[sequence]`

- [ ] 将页面结构拆成三个清晰区域：
  1. 顶部：返回、项目名、当前位置
  2. 中部：任务正文与来源
  3. 底部：备注与审核操作
- [ ] 将“第 N / 总条”和进度合并为紧凑的导航头，避免页头占据过多垂直空间。
- [ ] 任务正文区域减少装饰，使用接近阅读器的纯净纸面；正文宽度应独立于操作区并保持舒适行长。
- [ ] 对超长文本提供合理的最大高度与展开策略，避免操作按钮被推到首屏之外。
- [ ] 将“第 X 页”作为次要来源信息，与状态标签保持一条视觉基线。
- [ ] 备注默认保持低权重；任务已暂留或用户聚焦时再强调，减少它对主任务文本的干扰。
- [ ] 补充 `0 / 2000` 字符计数，并在接近上限时渐进提示。
- [ ] 将导航操作与审核结论明确分组：
  - 左侧/上层：上一条、下一条
  - 右侧/下层：暂留、通过
  - “通过”始终是唯一主按钮
- [ ] 桌面端使用底部 sticky action bar，长内容滚动时仍能操作；移动端使用安全区适配的固定底栏。
- [ ] 将“下一条/跳过”根据当前状态动态显示为“跳过”或“下一条”，降低语义歧义。
- [ ] 保存中只让被点击的按钮显示加载状态，同时禁用冲突操作，避免两个按钮都像正在保存。
- [ ] 保存成功使用短暂 Toast/轻触感反馈，不占据卡片固定空间；错误则保留为可操作的 inline notice。
- [ ] 为上一条/下一条增加键盘快捷键提示，并确保输入备注时不会误触发。
- [ ] 未保存备注离开时，用项目内 Modal 替代浏览器原生 `confirm`，统一视觉与文案。

## P1：结果页 `/projects/[projectId]/result`

- [ ] 删除重复表达：圆环进度和线性进度保留一种即可；建议保留克制的圆环或大号百分比。
- [ ] 四项统计改为统一的中性容器，通过小图标或局部文字色表达状态，不让四种彩色背景同时争夺注意力。
- [ ] 强化结果结论：
  - 全部处理完成时显示明确的完成标题和简洁成功反馈
  - 仍有待处理时，标题直接说明还剩多少项
- [ ] 统计数值使用更大的数字、更小的标签和稳定对齐，形成 Apple Health/Settings 式信息层级。
- [ ] 暂留与待处理列表提供更直接的后续入口，并在按钮文案中显示数量。
- [ ] 按钮区只保留一个主操作；“返回项目列表”等动作降为次级或文字按钮。
- [ ] 去除非交互统计块的 hover 位移动效，避免错误的可点击暗示。
- [ ] 移动端统计卡改为单列或紧凑 2×2 布局，确保标签不挤压、数字不换行。

## P1：导航、状态与细节

- [ ] 顶栏宽度与各页面内容网格统一，避免顶栏 `max-w-6xl`、首页 `max-w-4xl`、审核页 `max-w-3xl` 产生无意的左右跳动。
- [ ] 统一返回入口的样式和位置，审核页与结果页不要分别采用不同边距策略。
- [ ] 为页面切换、列表加载和任务预取增加轻量 Skeleton，避免空白闪烁。
- [ ] 深色模式单独校准：
  - 避免纯黑背景与高亮白卡片形成过强反差
  - 阴影改为边框/高光
  - 黄色和绿色降低亮度，保证文字对比度
- [ ] 统一图标线宽为 1.75–2px；装饰图标设置 `aria-hidden`，功能图标必须有文本或无障碍名称。
- [ ] 使用统一日期格式，如“2026年7月29日 16:13”，避免当前数字串显得偏工程化。
- [ ] 所有异步状态避免布局跳动，并为错误提供“重试”或“重新上传”等下一步。

## P1：响应式与可访问性

- [ ] 在 375px、768px、1024px、1440px 四个宽度分别验收首页、审核页、结果页和 404 页。
- [ ] 小屏首页标题与上传按钮改为上下排列，主按钮宽度适中，不让状态反馈挤压标题。
- [ ] 小屏项目行重新排序：标题/状态 → 进度 → 统计 → 操作，不简单依赖 `flex-wrap`。
- [ ] 小屏审核按钮保持单手可达，并适配 `env(safe-area-inset-bottom)`。
- [ ] 检查浅色、深色主题下正文、辅助文字、禁用文字和状态色的 WCAG AA 对比度。
- [ ] 所有可交互卡片、主题选项、按钮和输入框具备清晰且一致的键盘焦点。
- [ ] 校验 200% 浏览器缩放时无截断、遮挡和水平滚动。
- [ ] 为进度变化和保存结果提供合适的 `aria-live`，但避免重复播报。

## P2：提升精致度

- [ ] 为上传成功、审核通过和全部完成设计低干扰微动效，避免大幅弹跳或彩带式动画。
- [ ] 项目列表首屏可加入非常淡的径向光或材质渐变，但不能影响文字对比度。
- [ ] 空状态图形优先使用项目内 SVG/CSS，保持轻量、清晰和深色模式适配。
- [ ] 为常用操作增加克制的 tooltip 与快捷键提示。
- [ ] 补充 favicon、应用图标和页面 metadata，使浏览器标签页也保持统一品牌感。

## 建议实施顺序

- [ ] 阶段 1：Token、排版、圆角、阴影、按钮、卡片、输入框。
- [ ] 阶段 2：首页网格、项目列表、上传流程。
- [ ] 阶段 3：审核页阅读布局与 sticky 操作区。
- [ ] 阶段 4：结果页信息去重与状态表达。
- [ ] 阶段 5：深色模式、响应式、可访问性和微动效。

## 视觉验收标准

- [ ] 首屏 3 秒内可以看清“这是什么、当前有哪些项目、下一步做什么”。
- [ ] 每个页面只有一个最醒目的主操作，颜色不会同时争抢注意力。
- [ ] 静态内容没有虚假的 hover/悬浮反馈，可点击区域有明确反馈。
- [ ] 同级组件使用一致的圆角、间距、字号、阴影和图标线宽。
- [ ] 首页在 1440×900 下内容拥有明确视觉重心，不再显得缩在页面上方。
- [ ] 审核页在常见笔记本高度下无需滚动即可看到任务主体与主要审核操作；长内容场景仍可操作。
- [ ] 亮色与深色模式都具有清晰的表面层级，没有低对比文字或刺眼状态色。
- [ ] 375px 移动端无横向滚动、按钮不拥挤、固定操作区不遮挡内容。
- [ ] `npm run build`、`npm test` 通过，并保存首页、空状态、审核页、结果页、深色模式和移动端对比截图。
