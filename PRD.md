# PDF Task Review 产品需求文档（PRD）

## 1. 文档信息

| 项目 | 内容 |
| --- | --- |
| 产品名称 | PDF Task Review |
| 文档版本 | V2.0 |
| 产品形态 | Web 应用 |
| 技术方向 | Next.js |
| 目标版本 | 多用户协作版 |
| 文档状态 | 已实现，持续维护 |

## 2. 产品概述

PDF Task Review 是一个将 PDF 内容转换为待审核任务的 Web 应用。

用户上传一个 PDF 文件后，系统创建一个 Project，并按 PDF 中的文本顺序，将每个有效文本行转换为一个 Task。用户进入 Project 后，可逐条审核 Task；页面每次只展示一个 Task，并支持通过、备注后暂留、回到上一条以及跳到下一条。

### 2.1 产品目标

- 降低从 PDF 创建审核清单的操作成本。
- 让用户保持专注，每次只处理一条 Task。
- 保留审核状态、备注和操作进度，支持中断后继续。
- 支持在任务之间前后移动，避免线性审核过程不可回退。

### 2.2 核心概念

- **Project**：由一次 PDF 导入产生的审核项目，一个 PDF 对应一个 Project。
- **Task**：从 PDF 中解析出的一个有效文本行，按原文顺序归属于 Project。
- **Review**：用户逐条查看 Task，并对其执行通过、暂时遗留或跳过操作的过程。

## 3. 用户与使用场景

### 3.1 目标用户

需要将 PDF 中的条目、要求或清单逐项审核的个人用户或内部业务人员。

系统支持本地账号登录。项目负责人可以添加审核人或只读成员、分派任务；每位审核人的结论、备注、进度和最后停留位置相互独立。并发更新使用版本号检测冲突，所有结论变更写入不可覆盖的审核历史。

### 3.2 典型场景

1. 用户打开项目列表页并上传一个 PDF。
2. 系统解析 PDF，为文件创建 Project，并将每个有效文本行创建为 Task。
3. 用户点击 Project，进入逐条 Review 页面。
4. 用户通过当前 Task，或填写备注后将其暂时遗留。
5. 用户可以返回上一条复查，也可以跳过当前 Task 去下一条。
6. 用户退出后再次进入，系统从上次停留的位置继续。
7. 所有 Task 均被处理后，系统提示本轮审核完成，并展示审核结果汇总。

## 4. 产品范围

### 4.1 MVP 范围

- 上传单个 PDF 文件。
- 一个 PDF 创建一个 Project。
- 从可复制文本的 PDF 中提取文本。
- 每个有效文本行创建一个 Task。
- Project 列表、导入状态和审核进度展示。
- 每次只展示一个 Task 的逐条 Review。
- Task 通过。
- Task 填写备注后暂时遗留。
- 返回上一条。
- 跳到下一条。
- 自动保存审核状态、备注和当前位置。
- Project 审核结果汇总。
- PDF 解析失败及空内容提示。

### 4.2 暂不包含

- 扫描件 PDF 的 OCR 识别。
- 加密或需要密码的 PDF。
- 批量上传多个 PDF。
- PDF 原页面图像与 Task 对照预览。
- 自定义拆分规则、合并行或手动调整 Task 顺序。
- 导出审核报告。
- 删除或重新导入 Project。

## 5. 业务规则

### 5.1 PDF 导入规则

1. 每次仅允许上传一个 `.pdf` 文件。
2. 默认最大文件大小为 20 MB；该数值应配置化。
3. 一个成功提交的 PDF 对应一个 Project。
4. Project 默认名称取 PDF 文件名并去除扩展名。
5. 系统按 PDF 页码从前到后提取文本，再按页面中的文本行顺序创建 Task。
6. 每个 Task 必须保存全局顺序号，同时记录来源页码；可用时记录页内行号。
7. 仅包含空格、制表符或换行符的空白行不创建 Task。
8. 行首和行尾空白应去除；行内原有空格应保留。
9. 相同文本出现在不同行时，仍创建不同 Task，不自动去重。
10. MVP 不对跨行段落进行语义合并。PDF 中被提取为两行的内容会生成两个 Task。
11. 页眉、页脚、页码等只要被解析为非空文本行，MVP 也会生成 Task，不进行自动语义过滤。
12. 上传后 Project 先进入“解析中”；解析成功后进入“待审核”，失败则进入“导入失败”。
13. 若没有解析出任何有效文本行，导入失败，不创建可审核 Task，并向用户说明可能是扫描件或空白 PDF。
14. 加密 PDF、损坏 PDF 或非 PDF 伪装文件必须被拒绝，并显示可理解的失败原因。

> 说明：PDF 并不天然保存统一的“行”结构，实际行划分依赖解析库返回的文字位置。开发阶段需使用样例文件验证排序，并将“视觉上看到的一行”尽可能还原为一个 Task。

### 5.2 Task 状态

| 状态 | 含义 | 进入方式 |
| --- | --- | --- |
| 待处理（pending） | 尚未给出审核结论 | Task 初始状态，或用户仅执行跳过 |
| 已通过（passed） | 当前 Task 审核通过 | 点击“通过” |
| 暂时遗留（deferred） | 当前 Task 暂不通过，等待后续处理 | 填写备注并点击“暂时遗留” |

业务约束：

- “通过”和“暂时遗留”是审核结论。
- “跳过/下一条”是导航行为，不是审核结论，Task 保持原状态。
- 已通过或已暂时遗留的 Task 可以再次打开并修改结论。
- 将已暂时遗留的 Task 改为已通过时，历史备注默认保留，但不在主界面突出展示；用户可主动编辑或清空。
- 将已通过的 Task 改为暂时遗留时，必须填写备注。
- 每次状态或备注变更均记录更新时间。

### 5.3 Review 导航规则

1. Task 严格按照导入时的顺序号展示。
2. Review 页面每次只展示一个 Task。
3. 点击“通过”后保存状态，并自动进入下一条。
4. 点击“暂时遗留”后保存状态和备注，并自动进入下一条。
5. “暂时遗留”的备注为必填，去除首尾空白后不能为空。
6. 点击“上一条”仅切换到前一个 Task，不修改当前 Task 状态。
7. 点击“下一条/跳过”仅切换到后一个 Task，不修改当前 Task 状态。
8. 第一条 Task 的“上一条”按钮禁用。
9. 最后一条 Task 的“下一条/跳过”按钮禁用。
10. 在最后一条执行“通过”或“暂时遗留”后，进入审核结果页。
11. 用户在任意 Task 离开页面时，系统保存该 Task 为 Project 的最后停留位置。
12. 用户再次点击 Project 时，默认回到最后停留位置；若不存在，则进入第一条。
13. 浏览器刷新后仍应停留在当前 Task。
14. 快速连续点击操作按钮时，只接受一次有效提交，避免重复跳转或状态错乱。

### 5.4 Project 状态与进度

| 状态 | 判断规则 |
| --- | --- |
| 解析中 | PDF 已上传，Task 尚未生成完成 |
| 导入失败 | PDF 解析失败或未得到有效 Task |
| 待审核 | 解析成功，所有 Task 都是待处理 |
| 审核中 | 至少一条 Task 已处理，且仍存在待处理 Task |
| 已完成 | 不存在待处理 Task；即所有 Task 均为已通过或暂时遗留 |

Project 进度计算：

`已处理数量 = 已通过数量 + 暂时遗留数量`

`审核进度 = 已处理数量 / Task 总数 × 100%`

跳过的待处理 Task 不计入已处理数量。

## 6. 功能需求

### 6.1 Project 列表页

#### 页面目标

让用户创建 Project、了解导入情况，并快速进入或继续 Review。

#### 页面内容

- 页面标题。
- “上传 PDF”主按钮。
- Project 列表。
- 空状态引导。

每个 Project 卡片或列表项展示：

- Project 名称。
- 原始 PDF 文件名。
- 创建时间。
- Project 状态。
- Task 总数。
- 已通过、暂时遗留、待处理数量。
- 审核进度条和百分比。
- “开始审核”“继续审核”或“查看结果”入口。

#### 交互要求

- 点击可审核的 Project 进入 Review 页面。
- 解析中的 Project 不允许进入 Review，并显示加载状态。
- 导入失败的 Project 展示失败原因。
- 列表默认按创建时间倒序排列。

### 6.2 PDF 上传与解析

#### 上传入口

点击“上传 PDF”后打开文件选择器或上传弹窗。

#### 上传校验

- 文件扩展名必须为 `.pdf`。
- 文件 MIME 类型应为 `application/pdf`；服务端还应校验文件签名，不能只信任扩展名。
- 文件不得超过配置的大小限制。
- 文件为空时拒绝上传。

#### 上传反馈

- 上传中：显示文件名和上传进度或明确的加载状态。
- 解析中：提示“正在解析 PDF 并创建任务”。
- 成功：提示成功创建的 Task 数量，并允许进入 Review。
- 失败：展示原因及重新上传入口。

上传和解析期间不得因用户重复点击而创建重复 Project。

### 6.3 Task Review 页面

#### 页面目标

帮助用户集中审核当前 Task，并清晰掌握所在位置和整体进度。

#### 页面布局

页面至少包含：

- 返回 Project 列表入口。
- Project 名称。
- 当前进度，例如“第 12 / 80 条”。
- 已处理进度，例如“已处理 35 / 80”。
- 当前 Task 状态标签。
- 当前 Task 文本。
- 来源信息，例如“第 3 页”。
- 已有备注展示及编辑区域。
- “上一条”按钮。
- “下一条/跳过”按钮。
- “通过”按钮。
- “暂时遗留”按钮。

#### 通过

- 用户点击“通过”。
- 按钮进入保存中状态，其他审核按钮暂时禁用。
- 保存成功后显示短暂成功反馈并自动进入下一条。
- 保存失败时保留当前页面，提示重试，不得提前跳转。

#### 暂时遗留

- 用户点击“暂时遗留”时展示备注输入区；也可始终展示备注输入区，但只有暂留时强制校验。
- 备注支持多行文本，建议最大 2,000 个字符。
- 备注为空时不允许提交，并展示字段错误。
- 保存成功后自动进入下一条。
- 保存失败时保留用户已输入的备注并允许重试。

#### 上一条

- 点击后进入顺序号减一的 Task。
- 返回后展示该 Task 当前状态和已有备注。
- 不撤销任何已经保存的审核结果。

#### 下一条/跳过

- 点击后进入顺序号加一的 Task。
- 若当前 Task 为待处理，仍保持待处理。
- 若备注区存在未保存的改动，离开前弹出确认提示：“备注尚未保存，是否放弃修改并继续？”
- 若当前 Task 已有审核结论，跳转不改变其结论。

### 6.4 审核结果页

页面展示：

- Project 名称。
- Task 总数。
- 已通过数量。
- 暂时遗留数量。
- 待处理数量。
- 审核进度。
- “继续处理待处理任务”按钮；仅在存在待处理 Task 时展示。
- “查看暂时遗留任务”按钮；进入逐条 Review，并定位到第一条暂时遗留 Task。
- “返回项目列表”按钮。

若用户处理了最后一条，但前面仍有被跳过的待处理 Task，应提示“已到达最后一条，仍有 N 条待处理”，不能将 Project 标记为已完成。

### 6.5 异常与边界状态

- Project 不存在：展示 404 状态和返回列表入口。
- Task 不存在或不属于当前 Project：拒绝访问并返回有效位置。
- Project 尚在解析：Review 页面展示解析中状态，不请求 Task。
- 网络断开：提示保存失败，保留当前数据并允许重试。
- PDF 解析超时：Project 标记为导入失败，记录适合排查的内部错误，但不向用户暴露堆栈。
- 超长单行：Task 卡片自动换行，页面不产生横向溢出。
- PDF 包含大量行：Task 应分批写入数据库；Review 页面按当前 Task 获取，不一次加载全部正文。

## 7. 页面与用户流程

### 7.1 主流程

```text
Project 列表
    ↓ 上传 PDF
文件校验
    ↓
创建 Project（解析中）
    ↓
解析 PDF → 按行创建 Task
    ↓
Project（待审核）
    ↓ 点击 Project
逐条 Review
    ├─ 通过 → 保存 → 下一条
    ├─ 备注暂留 → 保存 → 下一条
    ├─ 上一条 → 前一个 Task
    └─ 下一条/跳过 → 后一个 Task，状态不变
    ↓ 到达末尾
审核结果页
```

### 7.2 继续审核流程

```text
用户离开 Review
    ↓
保存最后停留的 Task
    ↓
再次打开 Project
    ↓
回到最后停留的 Task
```

## 8. 数据模型建议

V2 在 Project 与 Task 之外增加 `User`、`Session`、`ProjectMember`、`TaskAssignment`、`TaskReview`、`ReviewProgress` 和 `ReviewEvent`。Task 只保存原始任务内容；个人审核状态不再以 Task 上的旧版字段为准。

### 8.1 Project

| 字段 | 类型示例 | 说明 |
| --- | --- | --- |
| id | UUID | Project 唯一标识 |
| name | string | Project 名称 |
| originalFileName | string | 原 PDF 文件名 |
| fileUrl / fileKey | string | 文件存储位置 |
| fileSize | integer | 文件大小，单位字节 |
| status | enum | parsing、failed、ready/reviewing、completed |
| parseError | string/null | 面向用户的解析失败原因 |
| totalTasks | integer | Task 总数 |
| passedTasks | integer | 已通过数量 |
| deferredTasks | integer | 暂时遗留数量 |
| pendingTasks | integer | 待处理数量 |
| lastTaskId | UUID/null | 最后停留的 Task |
| createdAt | datetime | 创建时间 |
| updatedAt | datetime | 更新时间 |

> Project 的计数字段可以实时聚合，也可以冗余存储以优化列表性能；如冗余存储，状态更新必须与 Task 更新保持一致。

### 8.2 Task

| 字段 | 类型示例 | 说明 |
| --- | --- | --- |
| id | UUID | Task 唯一标识 |
| projectId | UUID | 所属 Project |
| sequence | integer | Project 内从 1 开始的顺序号 |
| content | text | 当前行文本 |
| pageNumber | integer/null | 来源页码 |
| lineNumber | integer/null | 可获得时记录页内行号 |
| status | enum | pending、passed、deferred |
| remark | text/null | 备注 |
| reviewedAt | datetime/null | 最近一次形成审核结论的时间 |
| createdAt | datetime | 创建时间 |
| updatedAt | datetime | 更新时间 |

约束与索引：

- `projectId + sequence` 必须唯一。
- 为 `projectId + status` 建立索引，支持汇总和定位待处理/暂留任务。
- 删除 Project 时应级联处理所属 Task 和原始文件；该能力不在 MVP 用户界面中开放。

## 9. 接口建议

以下为逻辑接口，实际可使用 Next.js Route Handlers 或 Server Actions 实现。

| 方法 | 路径示例 | 用途 |
| --- | --- | --- |
| POST | `/api/projects` | 上传 PDF 并创建 Project |
| GET | `/api/projects` | 获取 Project 列表 |
| GET | `/api/projects/:projectId` | 获取 Project 详情和汇总 |
| GET | `/api/projects/:projectId/tasks/:sequence` | 获取指定顺序的 Task |
| PATCH | `/api/tasks/:taskId/review` | 更新 Task 状态和备注 |
| PATCH | `/api/projects/:projectId/progress` | 更新最后停留位置 |
| GET | `/api/projects/:projectId/tasks?status=pending` | 定位指定状态的 Task |

关键要求：

- 服务端校验 Task 必须属于指定 Project。
- Review 更新应具有幂等性。
- 状态更新、Project 计数及最后停留位置应在事务中处理，或采用可保证最终一致性的方案。
- 不允许客户端直接指定或篡改 Project 的汇总数量。

## 10. 非功能需求

### 10.1 性能

- Project 列表首屏在常规网络下应于 2 秒内可交互。
- Task 前后切换在接口正常时应于 500 毫秒内呈现；可预取相邻 Task。
- 单个包含 5,000 个有效文本行、20 MB 以内的 PDF 应能完成异步解析，不阻塞页面请求。
- 前端不得一次性渲染或传输全部 Task 内容。

### 10.2 可靠性

- 审核结果以服务端保存成功为准。
- 保存失败不得改变当前 Task 的本地已确认状态或自动跳转。
- PDF 解析任务应支持失败记录，避免 Project 长期停留在解析中。
- 同一 Task 的并发更新应采用更新时间、版本号或数据库事务避免旧请求覆盖新结果。

### 10.3 安全

- 服务端校验文件类型、大小和 PDF 文件签名。
- 文件名在展示前转义，防止 XSS。
- 原始 PDF 不应存放于可任意枚举的公开路径。
- 数据库查询必须校验资源归属；引入登录后，应按用户隔离 Project。
- 日志不得记录完整 PDF 内容和用户备注。

### 10.4 可用性与可访问性

- 所有操作按钮有明确文本，不只依赖图标。
- 状态不能只用颜色区分，必须配合文字。
- 键盘可聚焦所有操作控件，并有明显焦点样式。
- 页面切换 Task 后，应将焦点合理移动到 Task 标题或内容区域。
- 重要操作结果通过可被辅助技术读取的提示反馈。

### 10.5 兼容性

- 支持当前主流桌面版 Chrome、Edge 和 Safari。
- 页面应适配宽度不小于 375 px 的移动端，但 MVP 以桌面审核体验为优先。

## 11. 埋点与产品指标

建议记录以下事件：

- PDF 上传开始、成功、失败。
- PDF 解析成功、失败、耗时和生成 Task 数。
- Project 开始审核、继续审核、到达末尾。
- Task 通过、暂时遗留、跳过、返回上一条。
- Task 保存失败。

核心指标：

- PDF 导入成功率。
- 平均解析耗时。
- Project 审核开始率。
- Project 完成率。
- 单个 Task 平均审核耗时。
- 被跳过 Task 比例。
- 暂时遗留 Task 比例。

## 12. 验收标准

### 12.1 PDF 导入

- 给定一个包含 3 页、共 20 个有效文本行的可复制文本 PDF，导入后创建 1 个 Project 和 20 个 Task。
- Task 顺序与 PDF 页码及页面内文本顺序一致。
- 空白行不创建 Task，相同文本行不被去重。
- 上传非 PDF、超限 PDF、损坏 PDF、加密 PDF 时，系统拒绝或标记失败并展示明确原因。
- 上传纯扫描件且未解析出文本时，系统提示当前版本不支持该文件，并且不进入 Review。

### 12.2 Review

- 打开 Project 后每次只显示一个 Task。
- 点击“通过”后，当前 Task 变为已通过，并自动进入下一条。
- 未填写备注时不能将 Task 标记为暂时遗留。
- 填写备注并暂留后，状态和备注刷新页面后仍然存在。
- 点击“上一条”可以查看前一 Task，且不会修改任一 Task 的状态。
- 点击“下一条/跳过”后，当前待处理 Task 仍为待处理。
- 第一条不能继续向前，最后一条不能继续向后。
- 已处理 Task 可以重新进入并修改状态或备注。
- 保存失败时不自动跳到下一条，用户输入不丢失。

### 12.3 进度与恢复

- 已通过与暂时遗留均计入已处理进度，跳过不计入。
- 用户离开并重新进入 Project 后回到最后停留的 Task。
- 刷新 Review 页面后仍显示同一个 Task。
- 所有 Task 均已通过或暂留时，Project 状态变为已完成。
- 到达最后一条但仍有被跳过 Task 时，Project 不得显示为已完成，并可继续定位到待处理 Task。

## 13. 技术实现建议

建议采用以下实现方向，最终由技术设计确定：

- Next.js App Router + TypeScript。
- React Server Components 用于列表和初始数据加载，客户端组件处理 Review 交互。
- PostgreSQL 配合 Prisma 或同类 ORM 存储 Project 和 Task。
- PDF 文件使用对象存储；开发环境可使用本地存储。
- 使用支持文字坐标的 PDF 解析库，根据页码和纵向位置恢复文本行顺序。
- 较大 PDF 采用后台任务解析，避免上传请求长时间阻塞。
- 使用数据库事务维护 Task 状态和 Project 汇总。
- Review 路由建议包含当前 Task 的 `sequence`，例如 `/projects/:projectId/review/:sequence`，以支持刷新、前进后退和直接定位。

## 14. 待产品确认项

以下事项不影响 MVP 文档成立，但应在开发前最终确认：

1. 20 MB 的默认上传限制是否符合实际文件规模。
2. 是否需要在首版支持扫描件 OCR。
3. 是否需要过滤重复页眉、页脚和纯页码行。
4. 暂留 Task 改为通过后，备注应保留、自动清空还是进入历史记录。
5. 登录和不同用户之间的数据隔离已在 V2 实现。
6. 是否需要 Project 删除、重命名和重新解析能力。
7. 是否需要导出包含审核状态与备注的报告。

## 15. 后续版本方向

- OCR 支持扫描件。
- PDF 原文页面与当前 Task 联动高亮。
- 自定义行拆分、合并及过滤规则。
- Project 归档和批量管理。
- 审核报告导出为 PDF 或 Excel（CSV 已支持）。
- 任务评论与通知（成员、分派、操作历史已支持）。
